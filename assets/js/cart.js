(()=>{
  const KEY='forgeCartV1';
  const money=p=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',minimumFractionDigits:0}).format(p/100);
  const load=()=>{try{return (JSON.parse(localStorage.getItem(KEY)||'[]')||[]).map(x=>({...x,key:x.key||(x.briefId?`brief:${x.briefId}`:`slug:${x.slug}`)}))}catch{return []}};
  let cart=load();
  const save=()=>{localStorage.setItem(KEY,JSON.stringify(cart));window.dispatchEvent(new CustomEvent('forge:cart-updated'));};

  const backdrop=document.createElement('div');backdrop.className='forge-cart-backdrop';
  const drawer=document.createElement('aside');drawer.className='forge-cart-drawer';drawer.setAttribute('aria-label','Shopping basket');
  const toggle=document.createElement('button');toggle.className='forge-cart-toggle';toggle.type='button';toggle.innerHTML='<span class="cart-icon">🛒</span><span class="cart-label">Basket</span><span class="forge-cart-count">0</span>';
  const toast=document.createElement('div');toast.className='forge-cart-toast';toast.setAttribute('role','status');
  drawer.innerHTML='<div class="forge-cart-head"><h2>Your basket</h2><button class="forge-cart-close" type="button" aria-label="Close basket">×</button></div><div class="forge-cart-items"></div><div class="forge-cart-foot"><div class="forge-cart-total"><span>Total</span><strong>£0</strong></div><label class="forge-promo-label" for="forge-promo-code">Discount code</label><div class="forge-promo"><input id="forge-promo-code" name="promoCode" autocomplete="off" placeholder="e.g. WELCOME10" maxlength="50"></div><button class="forge-cart-checkout" type="button">Secure checkout</button><p class="forge-cart-note">Any valid code is checked securely when Stripe checkout opens.</p><p class="forge-cart-status" role="status"></p></div>';
  document.body.append(backdrop,drawer,toggle,toast);
  const countEl=toggle.querySelector('.forge-cart-count'),itemsEl=drawer.querySelector('.forge-cart-items'),totalEl=drawer.querySelector('.forge-cart-total strong'),checkoutBtn=drawer.querySelector('.forge-cart-checkout'),statusEl=drawer.querySelector('.forge-cart-status'),promoEl=drawer.querySelector('#forge-promo-code');
  const syncHeader=()=>document.querySelectorAll('.header-basket-count').forEach(el=>el.textContent=String(cart.length));
  const open=()=>{drawer.classList.add('open');backdrop.classList.add('open')};
  const close=()=>{drawer.classList.remove('open');backdrop.classList.remove('open')};
  toggle.addEventListener('click',open);backdrop.addEventListener('click',close);drawer.querySelector('.forge-cart-close').addEventListener('click',close);
  document.addEventListener('click',e=>{const h=e.target.closest('.header-basket-link');if(!h)return;e.preventDefault();open();});
  const flash=msg=>{toast.textContent=msg;toast.classList.add('show');clearTimeout(flash.t);flash.t=setTimeout(()=>toast.classList.remove('show'),1700)};
  const render=()=>{countEl.textContent=String(cart.length);syncHeader();totalEl.textContent=money(cart.reduce((s,x)=>s+Number(x.amount||0),0));checkoutBtn.disabled=!cart.length;if(!cart.length){itemsEl.innerHTML='<div class="forge-cart-empty"><strong>Your basket is empty.</strong><br><small>Add any documents you want, then pay once.</small></div>';return;}itemsEl.innerHTML=cart.map(x=>`<div class="forge-cart-item"><div><h3>${x.title}</h3><small>${x.category||'Forge Compliance'}</small><strong>${money(x.amount)}</strong></div><button class="forge-cart-remove" type="button" data-remove-key="${x.key}">Remove</button></div>`).join('')};
  itemsEl.addEventListener('click',e=>{const b=e.target.closest('[data-remove-key]');if(!b)return;const removed=cart.find(x=>x.key===b.dataset.removeKey);cart=cart.filter(x=>x.key!==b.dataset.removeKey);save();render();if(removed?.slug&&!removed?.briefId)document.querySelectorAll(`[data-cart-add][data-slug="${removed.slug}"]`).forEach(btn=>btn.classList.remove('added'))});
  const add=item=>{item.key=item.key||(item.briefId?`brief:${item.briefId}`:`slug:${item.slug}`);if(!item.slug||!item.title||!Number.isFinite(Number(item.amount))||Number(item.amount)<1)return false;if(cart.some(x=>x.key===item.key)){open();flash('Already in your basket');return false;}cart.push(item);save();render();flash('Added to basket');open();return true};
  document.addEventListener('click',e=>{const b=e.target.closest('[data-cart-add]');if(!b)return;e.preventDefault();const item={slug:b.dataset.slug,title:b.dataset.title,amount:Number(b.dataset.amount||0),category:b.dataset.category||''};if(add(item))b.classList.add('added')});
  window.addEventListener('forge:add-custom-item',e=>{const d=e.detail||{};add({slug:'custom-sop',briefId:d.briefId,title:d.title||'Custom SOP',amount:Number(d.amount||6900),category:'Custom document'})});
  checkoutBtn.addEventListener('click',async()=>{if(!cart.length)return;checkoutBtn.disabled=true;checkoutBtn.textContent='Opening secure checkout…';statusEl.textContent='';try{const promoCode=String(promoEl?.value||'').trim().toUpperCase();const r=await fetch('https://sswedojvkqvoqmwhfmkj.supabase.co/functions/v1/forge-create-checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:cart.map(x=>({slug:x.slug,briefId:x.briefId||undefined})),promoCode:promoCode||undefined})});const data=await r.json().catch(()=>({}));if(!r.ok||!data.url)throw new Error(data.error||'Checkout is temporarily unavailable.');location.href=data.url}catch(err){statusEl.textContent=err.message||'Checkout is temporarily unavailable.';checkoutBtn.disabled=false;checkoutBtn.textContent='Secure checkout'}});
  render();cart.forEach(x=>{if(!x.briefId)document.querySelectorAll(`[data-cart-add][data-slug="${x.slug}"]`).forEach(btn=>btn.classList.add('added'))});
  if(new URLSearchParams(location.search).get('basket')==='open')open();
})();